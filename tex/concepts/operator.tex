\subsection{Operators}\label{operator}\label{operators}

The \emph{operator} runs \keyword{commands} on \keyword{shells} on \keyword{machines} to to maximize an objective function that depends on the state of multiple machines. 